include ../mixins/logo.pug

header
  nav.flex.justify-between.bb.bg-light-blue.h3
    +hikr_logo("white hover-lightest-blue w4")

    .flex-grow.pv0.ph3.flex.items-center
      if user
        include logged_in
      else
        include not_logged_in